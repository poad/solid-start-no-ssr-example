{
  "name": "solid-start-no-ssr-example",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r --if-present lint",
    "lint-fix": "pnpm -r --if-present lint-fix"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.0.5",
      "onFail": "download"
    }
  }
}
